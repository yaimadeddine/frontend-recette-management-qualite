export class Type {
  id: number;
  libelle: string;
  image: string;


  constructor(id: number, libelle: string,image:string) {
    this.id = id;
    this.libelle = libelle;
    this.image = image;
  }
}
